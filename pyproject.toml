[project]
name = "engineering-notebook"
version = "0.1.0"
description = "Frank's personal engineering notebook"
readme = "README.md"
authors = [
    { name = "Frank Wei", email = "fwei1729@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0.3",
]

[project.scripts]
update_base_urls = "engineering_notebook.base_urls:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
